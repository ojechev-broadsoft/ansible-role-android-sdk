#!/usr/bin/expect

set timeout -1;
spawn {{ android_executable }} "{{ (android_sdk_tools_to_install + android_sdks_to_install) | join('" "') }}";
expect {
    "Accept" { exp_send "y\r" ; exp_continue }
    eof
}
